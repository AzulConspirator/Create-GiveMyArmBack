# Create : Give My Arm Back

Create mod has the netherite backpack, it has a full suit armor, cover the the whole arm. this also comes with a feature client-side that disables the First person arm renderer (probably for performance). this aims to disables that

made This specifically for my minimal armor texture pack work with no issue.
